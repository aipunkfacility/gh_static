# Green Hill Tours — Статический лендинг

Одностраничный сайт туристического агентства Green Hill Tours в Муйне, Вьетнам.  
Легкий, быстрый, с динамической загрузкой контента из JSON-файлов.

## 🚀 Быстрый старт

1. Склонируйте репозиторий
2. Откройте `index.html` в браузере или разверните на любом хостинге
3. Готово! Сайт работает без Node.js, npm и сборщиков

## 📁 Структура проекта

```

gh_static/
├── index.html              \# Главная страница
├── styles.css              \# Оптимизированные стили (~10KB вместо 3MB Tailwind)
├── data/                   \# JSON файлы с контентом
│   ├── site-meta.json      \# Метаданные: заголовки, номер WhatsApp
│   ├── excursions.json     \# Список экскурсий
│   ├── transport-categories.json  \# Категории транспорта
│   ├── transport-items.json       \# Варианты аренды байков/авто
│   ├── accommodations.json        \# Варианты проживания
│   ├── services.json              \# Дополнительные услуги
│   └── offices.json               \# Контакты офисов
├── js/
│   ├── config.js           \# Конфигурация (пути к JSON, retry настройки)
│   ├── utils.js            \# Вспомогательные функции (валидация, WhatsApp)
│   ├── main.js             \# Главный скрипт загрузки данных
│   ├── components/
│   │   └── cards.js        \# Карточки экскурсий/услуг/транспорта
│   └── sections/
│       ├── hero.js         \# Главный баннер
│       ├── popular.js      \# Популярные предложения
│       ├── excursions.js   \# Секция экскурсий
│       ├── transport.js    \# Секция транспорта
│       ├── accommodation.js \# Секция проживания
│       ├── services.js     \# Секция услуг
│       └── contacts.js     \# Секция контактов
└── images/                 \# Изображения (логотипы, фото)

```

## ✏️ Как добавить новую экскурсию

### Шаг 1: Откройте `data/excursions.json`

### Шаг 2: Добавьте новый объект в массив

```json
{
  "id": 8,
  "title": "Название экскурсии",
  "shortDescription": "Краткое описание (1-2 предложения)",
  "fullDescription": "Подробное описание экскурсии",
  "duration": "Полный день",
  "price": "от 50$",
  "included": [
    "Трансфер из отеля",
    "Обед",
    "Входные билеты"
  ],
  "highlights": [
    "Посещение храма",
    "Водопады",
    "Обед в ресторане"
  ],
  "isActive": true,
  "isPopular": false
}
```


### Обязательные поля:

- `id` — уникальный номер (больше чем у всех существующих)
- `title` — название экскурсии
- `shortDescription` — краткое описание для карточки
- `isActive` — `true` чтобы показать на сайте, `false` чтобы скрыть


### Опциональные поля:

- `isPopular: true` — показать в секции "Популярное"
- `price` — цена (если не указать, кнопка будет "Узнать цену")
- `duration` — длительность тура
- `included` — что входит в стоимость
- `highlights` — ключевые точки маршрута


### Шаг 3: Сохраните файл и обновите сайт

Изменения появятся автоматически после обновления страницы.

## 🚗 Как добавить новый транспорт

### Откройте `data/transport-items.json`

```json
{
  "id": 10,
  "categoryId": "moto",
  "title": "Yamaha Exciter 150",
  "useCases": "Для активной езды и дальних поездок",
  "benefits": [
    "Мощный двигатель 150cc",
    "Спортивная посадка"
  ],
  "specs": [
    "150cc",
    "Расход: 2.5л/100км"
  ],
  "isActive": true,
  "isPopular": true
}
```


### Значения `categoryId`:

- `"standard"` — Стандартные скутеры
- `"comfort"` — Комфортные скутеры
- `"maxi"` — Макси-скутеры
- `"moto"` — Мотоциклы
- `"car"` — Автомобили


## 🏠 Как добавить вариант проживания

### Откройте `data/accommodations.json`

```json
{
  "id": 3,
  "title": "Название отеля",
  "slogan": "Краткий слоган о месте",
  "territoryDescription": "Описание территории",
  "roomFeatures": [
    "Кондиционер",
    "Wi-Fi",
    "Холодильник"
  ],
  "atmosphere": "Описание атмосферы",
  "locationDescription": "Близко к пляжу",
  "address": "123 Nguyen Dinh Chieu, Mui Ne",
  "isActive": true
}
```


## 🛠️ Как добавить услугу

### Откройте `data/services.json`

```json
{
  "id": 5,
  "title": "Название услуги",
  "shortDescription": "Краткое описание услуги",
  "details": "Подробное описание",
  "isActive": true,
  "isPopular": false
}
```


## 📞 Как изменить номер WhatsApp

### Откройте `data/site-meta.json`

```json
{
  "mainTitle": "Муйне: Отдых без забот",
  "mainSubtitle": "Текст подзаголовка",
  "responseTimeText": "Отвечаем быстро с 8:00 до 22:00",
  "whatsappNumber": "84372733431"
}
```

Измените `whatsappNumber` на новый номер (формат: код страны + номер без +).

## 🚀 Деплой на Beget

### Вариант 1: Через панель управления Beget

1. Войдите в панель управления на [beget.com](https://beget.com)
2. Перейдите в **Файловый менеджер**
3. Откройте папку вашего домена (обычно `/public_html` или `/ваш-домен.ru/public_html`)
4. Загрузите все файлы проекта:
   - `index.html`
   - `styles.css`
   - папку `data/`
   - папку `js/`
   - папку `images/` (если есть)
5. Готово! Сайт доступен по вашему домену

### Вариант 2: Через FTP (FileZilla)

1. Скачайте [FileZilla](https://filezilla-project.org/) (бесплатный FTP-клиент)
2. Получите FTP-данные в панели Beget:
   - Хост: `ваш-логин.beget.tech`
   - Логин: ваш логин на Beget
   - Пароль: ваш пароль от Beget
   - Порт: `21`
3. Подключитесь через FileZilla
4. В правой части (сервер) откройте `/public_html`
5. В левой части (локальный компьютер) откройте папку с проектом
6. Перетащите все файлы из левой части в правую
7. Дождитесь завершения загрузки

### Вариант 3: Через SSH (для опытных пользователей)

```bash
# 1. Подключитесь к серверу
ssh ваш-логин@ваш-логин.beget.tech

# 2. Перейдите в папку сайта
cd ~/public_html/ваш-домен.ru/public_html

# 3. Клонируйте репозиторий (если проект на GitHub)
git clone https://github.com/username/gh_static.git .

# Или загрузите через scp с локального компьютера:
# scp -r /путь/к/проекту/* ваш-логин@ваш-логин.beget.tech:~/public_html/
```


### Обновление контента после деплоя

#### Через Файловый менеджер Beget:

1. Откройте **Файловый менеджер** → папку `data/`
2. Нажмите на нужный JSON файл (например, `excursions.json`)
3. Нажмите **Редактировать**
4. Внесите изменения
5. Нажмите **Сохранить**
6. Обновите страницу сайта — изменения сразу видны

#### Через FTP:

1. Откройте FileZilla
2. Найдите файл в папке `data/` на сервере
3. Нажмите правой кнопкой → **Просмотр/Правка**
4. Внесите изменения в текстовом редакторе
5. Сохраните файл — он автоматически загрузится на сервер

#### Через SSH/Git (если используете GitHub):

```bash
# На локальном компьютере:
git add data/
git commit -m "Добавлена новая экскурсия"
git push

# На сервере Beget:
ssh ваш-логин@ваш-логин.beget.tech
cd ~/public_html/ваш-домен.ru/public_html
git pull
```


### Настройка домена

1. В панели Beget перейдите в **Домены**
2. Если домен еще не привязан:
    - Нажмите **Добавить домен**
    - Укажите ваш домен (например, `greenhill-tours.com`)
    - Выберите папку: `/public_html`
3. Если домен уже привязан, просто загрузите файлы в его папку

### Проверка работы сайта

После загрузки откройте ваш домен в браузере:

```
https://ваш-домен.ru
```

Если видите "403 Forbidden" или "Index of /":

1. Убедитесь что файл `index.html` находится в корне папки домена
2. Проверьте права доступа к файлам (должны быть `644` для файлов, `755` для папок)

### Права доступа (если сайт не работает)

Через SSH выполните:

```bash
cd ~/public_html/ваш-домен.ru/public_html
chmod 644 *.html *.css *.json
chmod 755 data/ js/ images/
chmod 644 data/* js/* js/*/* images/*
```

Или через FileZilla:

1. Выделите все файлы
2. Правой кнопкой → **Права доступа к файлу**
3. Установите `644` для файлов, `755` для папок

## 🔄 Автоматическое обновление через Git

Если проект на GitHub, настройте автообновление:

```bash
# На сервере Beget создайте скрипт обновления
nano ~/update-site.sh

# Вставьте:
#!/bin/bash
cd ~/public_html/ваш-домен.ru/public_html
git pull origin main

# Сохраните (Ctrl+X, Y, Enter)
chmod +x ~/update-site.sh

# Теперь для обновления просто запускайте:
~/update-site.sh
```


## 🐛 Решение проблем на Beget

### JSON файлы не загружаются (CORS ошибка)

Убедитесь что файлы имеют правильные MIME-типы. Создайте `.htaccess` в корне:

```apache
AddType application/json .json
```


### Медленная загрузка

Включите сжатие. Добавьте в `.htaccess`:

```apache
<IfModule mod_deflate.c>
  AddOutputFilterByType DEFLATE text/html text/css application/javascript application/json
</IfModule>
```


### Кеширование старых данных

Добавьте в `.htaccess`:

```apache
<FilesMatch "\.(json)$">
  Header set Cache-Control "max-age=300, must-revalidate"
</FilesMatch>
```

## 🐛 Решение проблем

### Сайт не загружается

1. Откройте консоль браузера (F12)
2. Проверьте ошибки загрузки JSON файлов
3. Убедитесь что все JSON файлы валидны (используйте [jsonlint.com](https://jsonlint.com))

### Элемент не отображается

1. Проверьте что `isActive: true` в JSON
2. Проверьте наличие всех обязательных полей
3. Посмотрите консоль — валидатор покажет предупреждения

### WhatsApp кнопка не работает

1. Проверьте формат номера в `data/site-meta.json` (без + и пробелов)
2. Пример правильного формата: `"84372733431"` (Вьетнам)

### Мобильное меню не открывается

1. Убедитесь что `js/utils.js` загружен
2. Проверьте что нет ошибок в консоли браузера

## 📝 Требования

- Современный браузер (Chrome, Firefox, Safari, Edge)
- Нет необходимости в Node.js, npm или других зависимостях
- Работает полностью на клиентской стороне


## 🔧 Технологии

- Vanilla JavaScript (ES6+)
- Custom CSS (~10KB, без фреймворков)
- Font Awesome иконки
- JSON для хранения данных


## 📄 Лицензия

© 2024 Green Hill Tours. Все права защищены.

## 🤝 Поддержка

По вопросам разработки и поддержки сайта:

- WhatsApp: +84 372 733 431
- Email: info@greenhill-tours.com

```

